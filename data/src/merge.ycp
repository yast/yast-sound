{
    map mod_idx	    = (map) SCR::Read(.target.ycp, "./src/module_index.ycp");
    map cards	    = (map) SCR::Read(.target.ycp, "./cardlist.ycp");
    map mods	    = (map) SCR::Read(.target.ycp, "./modlist.ycp");
    map idlist	    = (map) SCR::Read(.target.ycp, "./idlist.ycp");
    map vendors	    = (map) SCR::Read(.target.ycp, "./vendors.ycp");

    map db = $[
	"cards"	    :   cards,
	"modules"   :	mods,
	"indices"   :	idlist,
	"mod_idx"   :	mod_idx,
	"vendors"   :	vendors
    ];

    SCR::Write(.target.ycp, "./sndcards.ycp.in", db);
}
